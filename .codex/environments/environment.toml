# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "FiscoPortfolio"

[setup]
script = '''
# 1. Install dependencies
# Using 'npm install' is fine, but 'npm ci' is cleaner for automated setups
npm install

# 2. Environment Variables
# Ensures your app doesn't crash due to missing config
if [ -f .env.example ]; then
  cp .env.example .env.local
fi

# 3. Clean up any stale build artifacts 
# Vite can sometimes cache old versions of your code
rm -rf node_modules/.vite

# 4. Database/Schema generation (Optional)
# If you are using Prisma or a similar ORM, add it here:
# npx prisma generate
'''
